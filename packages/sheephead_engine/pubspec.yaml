name: sheephead_engine
description: Pure-Dart Sheephead game engine. No UI, no I/O, no Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

# Marks this package as a member of the workspace defined at the repo root.
resolution: workspace

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
